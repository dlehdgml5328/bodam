openapi: 3.0.3
info:
  title: Admin API
  version: 1.0.0
paths:
  /admin/search:
    get:
      summary: Search resources
      parameters:
        - in: query
          name: q
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Results
  /admin/refunds:
    get:
      summary: List refunds pending
      responses:
        '200':
          description: Refunds
