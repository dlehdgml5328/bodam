apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-semantic-exporter
  labels:
    app: redis-semantic-exporter
    component: semantic
    part-of: bodam
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-semantic-exporter
  template:
    metadata:
      labels:
        app: redis-semantic-exporter
        component: semantic
        part-of: bodam
    spec:
      containers:
        - name: redis-exporter
          image: oliver006/redis_exporter:latest
          env:
            - name: REDIS_ADDR
              value: redis://redis-semantic-service:6379
          ports:
            - containerPort: 9121
              name: metrics
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-semantic-exporter
  labels:
    app: redis-semantic-exporter
    component: semantic
    part-of: bodam
spec:
  selector:
    app: redis-semantic-exporter
  ports:
    - name: metrics
      port: 9121
      targetPort: metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redis-semantic-exporter
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: redis-semantic-exporter
  endpoints:
    - port: metrics
      interval: 30s
  namespaceSelector:
    matchNames:
      - default
