apiVersion: v1
kind: Service
metadata:
  name: redis-queue-service
  labels:
    app: redis-queue
    component: queue
    part-of: bodam
spec:
  clusterIP: None
  selector:
    app: redis-queue
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
