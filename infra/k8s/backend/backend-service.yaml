apiVersion: v1
kind: Service
metadata:
  name: bodam-backend
  namespace: default
  labels:
    app: bodam-backend
spec:
  type: ClusterIP
  selector:
    app: bodam-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
